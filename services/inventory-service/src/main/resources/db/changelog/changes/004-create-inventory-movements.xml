<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="004-create-inventory-movements" author="pharmaops">
        <createTable tableName="inventory_movements">
            <column name="id" type="UUID">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="product_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="store_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="batch_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <!-- SALE_OUTFLOW, RESTOCK_INFLOW, ADJUSTMENT, RETURN -->
            <column name="type" type="VARCHAR(30)">
                <constraints nullable="false"/>
            </column>
            <column name="quantity" type="INTEGER">
                <constraints nullable="false"/>
            </column>
            <!-- Propaga o correlationId do evento Kafka para rastreabilidade -->
            <column name="correlation_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="created_at" type="TIMESTAMP WITH TIME ZONE" defaultValueComputed="now()">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addForeignKeyConstraint
                baseTableName="inventory_movements"
                baseColumnNames="product_id"
                constraintName="fk_movements_product"
                referencedTableName="products"
                referencedColumnNames="id"/>

        <addForeignKeyConstraint
                baseTableName="inventory_movements"
                baseColumnNames="batch_id"
                constraintName="fk_movements_batch"
                referencedTableName="batches"
                referencedColumnNames="id"/>
    </changeSet>

</databaseChangeLog>
