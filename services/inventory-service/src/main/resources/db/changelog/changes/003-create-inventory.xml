<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <changeSet id="003-create-inventory" author="pharmaops">
        <createTable tableName="inventory">
            <column name="product_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <!-- store_id vem do identity-service; sem FK entre bancos (ADR-003) -->
            <column name="store_id" type="UUID">
                <constraints nullable="false"/>
            </column>
            <column name="quantity" type="INTEGER" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
            <column name="minimum_quantity" type="INTEGER" defaultValueNumeric="0">
                <constraints nullable="false"/>
            </column>
        </createTable>

        <addPrimaryKey
                tableName="inventory"
                columnNames="product_id, store_id"
                constraintName="pk_inventory"/>

        <addForeignKeyConstraint
                baseTableName="inventory"
                baseColumnNames="product_id"
                constraintName="fk_inventory_product"
                referencedTableName="products"
                referencedColumnNames="id"/>
    </changeSet>

</databaseChangeLog>
